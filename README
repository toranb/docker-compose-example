git clone https://github.com/toranb/docker-compose-example.git

mv docker-compose-example example

cd example

docker-compose build

docker-compose up --scale app=2

note: .env file is required to spin up Postgres

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=foo
